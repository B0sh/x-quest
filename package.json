{
  "name": "x-quest",
  "version": "1.5.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/B0sh/x-quest"
  },
  "scripts": {
    "clean": "rimraf dist",
    "start": "npm run clean && parcel index.html",
    "watch": "npm run clean && parcel index.html --public-url /projects/x-quest/dist",
    "build": "npm run clean && parcel build index.html",
    "ww": "npm run clean && parcel build index.html --no-source-maps --public-url /projects/x-quest/dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "howler": "^2.2.4",
    "rot-js": "^2.2.1",
    "uuid": "^11.0.5"
  },
  "devDependencies": {
    "@types/howler": "^2.2.12",
    "parcel": "^2.8.3",
    "parcel-reporter-static-files-copy": "^1.5.0",
    "process": "^0.11.10",
    "rimraf": "^3.0.2",
    "typescript": "^5.7.3"
  }
}
